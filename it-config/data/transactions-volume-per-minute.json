{
  "id": "transactions-volume-per-minute",
  "name": "Transaction volume per minute",
  "provider": "jdbc",
  "datasource": "prod-like-sqlite",
  "sql": "SELECT strftime('%Y-%m-%dT%H:%M:00', created_at) AS minute, SUM(amount_cents) AS volume_cents FROM transactions GROUP BY minute ORDER BY minute",
  "cacheTtlSeconds": 0
}
