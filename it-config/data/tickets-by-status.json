{
  "id": "tickets-by-status",
  "name": "Tickets by status",
  "provider": "jdbc",
  "datasource": "prod-like-sqlite",
  "sql": "SELECT status, COUNT(*) AS ticket_count FROM tickets GROUP BY status",
  "cacheTtlSeconds": 0
}
