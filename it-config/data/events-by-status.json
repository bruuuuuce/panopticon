{
  "id": "events-by-status",
  "name": "Async events by status",
  "provider": "jdbc",
  "datasource": "prod-like-sqlite",
  "sql": "SELECT status, COUNT(*) AS event_count FROM event_outbox GROUP BY status",
  "cacheTtlSeconds": 0
}
