{
  "id": "fault-empty-filtered",
  "name": "[fault injection] Query against a populated table, filtered to zero rows",
  "provider": "jdbc",
  "datasource": "prod-like-sqlite",
  "sql": "SELECT * FROM tickets WHERE status = 'NOT_A_REAL_STATUS'",
  "cacheTtlSeconds": 0
}
