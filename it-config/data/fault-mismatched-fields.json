{
  "id": "fault-mismatched-fields",
  "name": "[fault injection] Result columns don't match the panel's configured field names",
  "provider": "jdbc",
  "datasource": "prod-like-sqlite",
  "sql": "SELECT status AS state, COUNT(*) AS n FROM tickets GROUP BY state",
  "cacheTtlSeconds": 0
}
