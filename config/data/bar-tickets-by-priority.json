{
  "id": "bar-tickets-by-priority",
  "name": "Tickets by priority",
  "provider": "jdbc",
  "datasource": "demo-h2",
  "sql": "SELECT priority, COUNT(*) AS ticket_count FROM tickets GROUP BY priority ORDER BY priority",
  "timeoutMs": 5000,
  "maxRows": 20
}
