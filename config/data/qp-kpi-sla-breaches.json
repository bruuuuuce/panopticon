{
  "id": "qp-kpi-sla-breaches",
  "name": "Tickets resolved outside 24h SLA",
  "provider": "jdbc",
  "datasource": "demo-h2",
  "sql": "SELECT COUNT(*) AS sla_breaches FROM tickets WHERE resolved_at IS NOT NULL AND DATEDIFF('HOUR', created_at, resolved_at) > 24",
  "timeoutMs": 5000,
  "maxRows": 1
}
