{
  "id": "line-tickets-per-day",
  "name": "Tickets created per day (last 14 days)",
  "datasource": "mock",
  "sql": "SELECT CAST(created_at AS DATE) AS day_bucket, COUNT(*) AS ticket_count FROM tickets WHERE created_at >= DATEADD('DAY', -14, CURRENT_TIMESTAMP) GROUP BY CAST(created_at AS DATE) ORDER BY day_bucket",
  "timeoutSeconds": 5,
  "maxRows": 30
}
