{
  "id": "donut-tickets-by-priority",
  "name": "Open tickets by priority",
  "datasource": "mock",
  "sql": "SELECT priority, COUNT(*) AS ticket_count FROM tickets WHERE status <> 'CLOSED' GROUP BY priority ORDER BY priority",
  "timeoutSeconds": 5,
  "maxRows": 20
}
