{
  "id": "payments-kpi-revenue-today",
  "name": "Revenue today",
  "datasource": "mock",
  "sql": "SELECT COALESCE(SUM(amount), 0) AS revenue_today FROM payments WHERE status = 'SUCCEEDED' AND CAST(created_at AS DATE) = CURRENT_DATE",
  "timeoutSeconds": 5,
  "maxRows": 1
}
